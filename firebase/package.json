{
  "name": "firebase-rules",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Regras de Firestore/Storage + suíte de testes com emulator",
  "scripts": {
    "test": "vitest run",
    "lint": "echo 'no lint'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "echo 'nothing to build'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^10.13.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
